// Jest bootstrap referenced by jest.conf.js (setupFiles). The current specs
// are pure-function tests (formatters, request/response normalisers), so no
// globals are patched here yet — keep this file as the single place to add
// jsdom polyfills when component tests appear.
export {};
