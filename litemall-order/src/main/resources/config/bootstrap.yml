spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://localhost:8888
      #uri: http://configserver:8888
    bootstrap:
      enabled: true
    compatibility-verifier:
      enabled: false


eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
