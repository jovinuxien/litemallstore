<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  Hand-written mapper for litemall_article_category (article CMS, V36).
  NOT MyBatis-Generator output. Relies on the global mapUnderscoreToCamelCase=true
  (set on the SqlSessionFactory) so resultType columns map snake_case -> camelCase.
-->
<mapper namespace="org.linlinjava.litemall.db.dao.ArticleCategoryMapper">

  <sql id="columns">
    id, name, sort_order, add_time, update_time, deleted
  </sql>

  <insert id="insert" parameterType="org.linlinjava.litemall.db.domain.LitemallArticleCategory"
          useGeneratedKeys="true" keyProperty="id">
    insert into litemall_article_category
      (name, sort_order, add_time, update_time, deleted)
    values
      (#{name}, #{sortOrder}, #{addTime}, #{updateTime}, #{deleted})
  </insert>

  <select id="selectById" resultType="org.linlinjava.litemall.db.domain.LitemallArticleCategory">
    select <include refid="columns" />
    from litemall_article_category
    where id = #{id} and deleted = 0
  </select>

  <select id="selectAll" resultType="org.linlinjava.litemall.db.domain.LitemallArticleCategory">
    select <include refid="columns" />
    from litemall_article_category
    where deleted = 0
    order by sort_order asc, id asc
  </select>

  <update id="updateSelective" parameterType="org.linlinjava.litemall.db.domain.LitemallArticleCategory">
    update litemall_article_category
    <set>
      <if test="name != null">name = #{name},</if>
      <if test="sortOrder != null">sort_order = #{sortOrder},</if>
      update_time = #{updateTime},
    </set>
    where id = #{id} and deleted = 0
  </update>

  <update id="logicalDeleteById">
    update litemall_article_category
    set deleted = 1, update_time = #{updateTime}
    where id = #{id} and deleted = 0
  </update>

</mapper>
